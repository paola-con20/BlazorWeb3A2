@page "/Grafica"

<div class="container">

    <section class="top-area">

        <div class="box info-box-left">
            <i class="fas fa-chart-line icon-large primary-color"></i>
            <h3><span class="highlight">Métricas</span> Clave</h3>
            <p>
                Aquí se presentan los **indicadores más relevantes** del período.
                Muestra un dato destacado como <span class="data-value">25.3K</span> visitas
                o <span class="data-value">$4500</span> de ingresos esta semana.
            </p>
        </div>

        <div class="box info-box-right">
            <i class="fas fa-users icon-large secondary-color"></i>
            <h3><span class="highlight">Análisis</span> de Contexto</h3>
            <p>
                Este recuadro es ideal para información **cualitativa** o un resumen
                ejecutivo sobre la **adopción de usuarios** y los próximos objetivos estratégicos.
            </p>
        </div>
    </section>

    <section class="chart-area">
        <h3><i class="fas fa-chart-bar"></i> Visualización de Datos Central</h3>

        <div class="chart-wrapper">
            <canvas id="myChart"></canvas>
        </div>

        <p class="chart-description">
            *Nota: Esta gráfica muestra la tendencia de los últimos 30 días, con un enfoque en la actividad diaria.*
        </p>
    </section>

</div>

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // Llama a la función JavaScript para inicializar la gráfica
            await JS.InvokeVoidAsync("initChart");
        }
    }

    [Inject] private IJSRuntime JS { get; set; } = default!;
}